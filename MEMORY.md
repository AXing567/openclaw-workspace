# MEMORY.md

## Long-term memory

- 用户叫阿星，默认这样称呼。
- 用户自述人格倾向为 INTJ。
- 我的名字是“虾丞相”。
- 用户偏好：在内部可自主推进的事情上，默认直接做完，不要频繁停下来征求“是否同意”；除非涉及外部动作、敏感操作或高风险变更。
- 工具经验：当前已打通“命令行联网 + 本地浏览器自动化 + 任务状态智能汇报”的组合能力。适合用于：公开网页资料搜索、网页结构分析、接口/请求观察、定制化页面探测、半自动抓取原型验证、需要浏览器真实打开页面的复杂调研。
- 使用边界：这套能力适合做定向网页攻坚和执行器搭建，不等于稳定低成本的通用 Web 搜索工具；遇到社媒风控（如小红书 IP 风险）、系统依赖缺失、浏览器运行库问题时，要优先判断为环境/风控问题，而不是误判为整体方案不可行。
- 方法经验：在受限环境下，优先用“无 sudo 本地补依赖 + 手动下载浏览器 + Playwright 指向本地 executablePath + LD_LIBRARY_PATH”方案打通浏览器自动化；比反复等待系统级安装权限更可控。
- 工作流要求：长任务不能只靠口头承诺回报。必须有“任务状态文件 + 监督心跳 + 阻塞态更新 + 可见沉淀”四件套，避免黑盒和复读型汇报。
- 回报机制经验：单纯 cron 定时读状态文件会复读，不能算真正监督。更合理的是“任务监督心跳”：比较状态是否变化、识别连续无变化、明确提示疑似卡住/存在阻塞。
- 可见性要求：只要更新长期记忆或关键文档，必须让用户能在 /home/ax/.openclaw/workspace/MEMORY.md 或相关文件中直接看到，不要只在口头上说“记住了”。
- 用户判断：基于手动维护 current-task.json 的监督/回报机制不可靠，后续不要再用“维护状态 JSON + 定时复读/监督”作为长任务回报方案。

- OpenClaw 工具使用原则：优先采用内置一流工具而不是旧式技能或 shell 绕路；网页交互默认走 `browser -> snapshot -> act`；长任务按 `exec + process` 的前后台模型理解；工具可用性按 `tools.profile -> tools.byProvider -> tools.allow/deny` 分层判断。

- OpenClaw 能力增强原则：轻量网页获取优先 `web_search/web_fetch`，重交互或 JS/反爬页面切 `browser`；Skills 是任务流程固化层，Plugin 是平台能力扩展层；Skills 要控制数量和 token 成本，并按 workspace/shared/bundled 分层管理；host 执行必须同时按工具策略、exec 审批和 allowlist 三层理解。

- Sub-agent 使用原则：`sessions_spawn` 本质是非阻塞的独立会话派生；适合长任务、慢任务、可并行任务，不适合同步小修补。子代理默认不继承 `SOUL.md`、`USER.md`、`MEMORY.md` 等人格/长期记忆上下文，凡是依赖这些信息的任务都必须在 task 中显式补充。
- 心跳学习原则：当 `HEARTBEAT.md` 只要求空闲时提升 OpenClaw 智力时，默认做低打扰、低 token 的本地沉淀；优先整理现有知识并压缩成规则/选路卡片/边界说明，避免在心跳里展开大工程。
- 心跳执行约束：若当前工具策略限制 shell/目录遍历，heartbeat 不应为“找资料”反复试探；应退化为整理已知规则、补充选路经验、修正文档表述，产出以 workspace 内可见文本沉淀为准。
- 深夜心跳约束：在 23:00-08:00 的本地时段，即使 HEARTBEAT.md 要求空闲学习，也默认选择最低打扰动作；优先写短日志、补规则、压缩经验，不主动展开高噪音探索或任何外部动作。
- 深夜最小闭环原则：深夜 heartbeat 若已完成“一次判断 + 一次本地沉淀”，就应立即停止，不因环境安静而连续追加第二轮微扩张；目标是单轮闭环，不是静默加班。
- 空闲学习选路卡：heartbeat 学习默认优先级为“先读本地长期记忆/MEMORY.md → 提炼一条可复用规则 → 只在确有新增价值时落盘”；若本地规则已足够且本轮无明确新增，就直接返回 `HEARTBEAT_OK`，避免把‘每三十分钟一次’误执行成机械重复写日志。
- Heartbeat 去重规则：若本轮仅能从既有 HEARTBEAT/MEMORY 里得到与上一轮同级别结论，且没有新资料、新观察、新压缩表达，就不要为‘形式上完成学习’再写一条等价记录；直接 `HEARTBEAT_OK`。
