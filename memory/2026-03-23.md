# 2026-03-23

- 08:24 心跳：根据 HEARTBEAT.md 执行空闲学习。当前环境下 `exec` 被 allowlist 拦截，无法用 shell 做本地 docs 枚举；因此将 heartbeat 学习策略沉淀为长期规则：遇到工具受限时，不反复试探目录/命令，退化为整理已知 OpenClaw 规则与执行约束，并以 workspace 可见文件为交付。
- 08:54 心跳：新增 `docs/openclaw-tool-routing-card.md`，把 OpenClaw 常用工具的默认选路、禁忌用法和边界判断压缩成一张短卡，便于后续在受限环境下快速决策。
- 09:24 心跳：新增 `docs/openclaw-execution-modes-card.md`，把“当前会话直做 / exec+process / sessions_spawn / ACP / heartbeat”五类执行模式的适用边界和注意事项压缩成卡片，减少后续工具误用。
- 11:11 心跳：新增 `docs/openclaw-memory-recall-card.md`，把 `memory_search` 的必跑场景、推荐流程和边界判断压缩成卡片，减少后续回答中漏检历史记忆的问题。
